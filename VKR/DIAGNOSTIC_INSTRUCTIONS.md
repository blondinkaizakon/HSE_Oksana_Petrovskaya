# Инструкция по диагностике: приложение не отвечает на запросы

## 🔍 Пошаговая диагностика

### Шаг 1: Проверка в браузере (DevTools)

1. Откройте приложение в браузере
2. Нажмите **F12** (откроется DevTools)
3. Перейдите на вкладку **Console**
4. Отправьте сообщение в чат
5. **Проверьте логи в консоли:**

   **Должно быть:**
   ```
   🔧 Используется локальная модель Qwen на: http://...
   📤 Отправка запроса к Qwen API: http://...
   📥 Получен ответ, статус: 200 OK
   ```

   **Если есть ошибки - скопируйте их!**

### Шаг 2: Проверка Network (запросы)

1. В DevTools перейдите на вкладку **Network**
2. Отправьте сообщение в чат
3. Найдите запрос к `/generate`
4. **Проверьте:**
   - **Статус** (должен быть 200)
   - **URL** запроса
   - **Ошибки** (если есть)

### Шаг 3: Проверка API

```bash
# Проверка здоровья API
curl http://localhost:8001/health

# Должен вернуть:
# {"status":"ok","model_loaded":true,"model_name":"Qwen/Qwen2.5-1.5B-Instruct"}
```

### Шаг 4: Проверка логов API

```bash
cd /root/qwen-model
tail -50 api_server.log | grep -E "POST.*generate|ERROR"
```

## 🐛 Частые проблемы

### 1. Ошибка "Failed to fetch" / "NetworkError"

**Причина:** Браузер не может обратиться к API

**Решения:**
1. Проверьте, что API запущен:
   ```bash
   ps aux | grep uvicorn | grep 8001
   ```

2. Перезапустите API:
   ```bash
   cd /root/qwen-model
   source venv/bin/activate
   python -m uvicorn api.main:app --host 0.0.0.0 --port 8001
   ```

3. Проверьте, что API доступен:
   ```bash
   curl http://localhost:8001/health
   ```

### 2. Ошибка CORS

**Причина:** CORS не настроен правильно

**Решение:** Проверьте, что в `qwen-model/api/main.py` есть:
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    ...
)
```

### 3. Таймаут (90 секунд)

**Причина:** Модель работает медленно на CPU

**Решение:** Это нормально. Генерация занимает 15-30 секунд на CPU. Подождите.

### 4. Неправильный URL API

**Причина:** Приложение использует неправильный URL

**Решение:** 
- Если приложение открыто по `http://IP:3000` - API должен быть доступен по `http://IP:8001`
- Если приложение открыто по `http://localhost:3000` - API должен быть доступен по `http://localhost:8001`

## 📝 Что проверить

- [ ] API запущен на порту 8001
- [ ] API отвечает на `/health`
- [ ] В консоли браузера нет ошибок CORS
- [ ] В Network виден запрос к `/generate` со статусом 200
- [ ] В логах API нет ошибок

## 🆘 Если ничего не помогает

1. **Проверьте логи браузера (Console)**
2. **Проверьте логи API:**
   ```bash
   tail -100 /root/qwen-model/api_server.log
   ```
3. **Проверьте логи приложения:**
   ```bash
   tail -50 /root/blondinkaizakon/dev.log
   ```
4. **Скопируйте все ошибки для анализа**

